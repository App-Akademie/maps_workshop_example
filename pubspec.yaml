name: maps_workshop_example
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  cloud_firestore: ^4.16.0
  firebase_core: ^2.28.0
  flutter:
    sdk: flutter
  flutter_osm_plugin: ^0.70.4
  geolocator: ^11.0.0
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
